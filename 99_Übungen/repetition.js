/**
 * Datentypen
 */

/**
 * 1. Weise einer Variable `Stadt` einen beliebigen Schweizer Städtenamen zu und gib sie in der Konsole aus.
 */


/**
 * 2. Erstelle eine Variable `Land` für das entsprechende Land und gib sie in der Konsole aus.
 */


/**
 * 3. Verknüpfe `Land` und `Stadt` zu einem Satz wie "Die Stadt Paris liegt in Frankreich" und gib ihn aus.
 */


/**
 * Vergleiche
 */

/**
 * 1. Nutze den `typeof`-Operator, um den Datentyp von `x` und `y` zu überprüfen.
 */
let x = '5';
let y = 'String';

// Vergleiche 5 und '5' mit == und ===
// Gib das Ergebnis aus und erkläre den Unterschied

// Vergleiche 'String' und 'String' mit == und ===
// Gib das Ergebnis aus und erkläre den Unterschied

// Vergleiche `null` und `undefined` mit == und ===
// Gib das Ergebnis aus und erkläre den Unterschied

/**
 * 2. Programmiere eine Logik, die, wenn `temp` unter -10 liegt, ausgibt: Es friert bei ${temp}°C!
 */
let temp = -15;

/**
 * 3. Programmiere eine Logik, die:
 * - Wenn `licht` auf 'orange' steht, 'Achtung!' ausgibt
 * - Wenn `licht` auf 'rot' steht, 'Stopp!' ausgibt
 * - Wenn `licht` auf 'grün' steht, 'Sicher!' ausgibt
 * 
 */
let licht = "rot";

/**
 * Arrays
 */

/**
 *  1. Erstelle ein Array mit Schweizer Städten, iteriere darüber und gib jede Stadt aus.
 */
let cities = [];

/**
 *  2. Füge dem Array eine neue Stadt hinzu und gib die aktualisierte Länge aus.
 */

/**
 * Schleifen
 */

/**
 *  1. Schreibe eine Schleife, die das Produkt aller Zahlen in einem Array berechnet.
 */
let numbers = [2, 3, 4, 5, 6, 7];

/**
 * 2. Erstelle ein Array mit Noten, berechne deren Durchschnitt mit einer for-Schleife und gib aus, ob die Durchschnittsnote eine ausreichende Note ist (>= 4).
 */

/**
 * Funktionen
 */

/**
 * Schreibe eine Funktion `multiply`, die:
 * 
 * - Zwei Zahlen als Eingaben nimmt
 * - Ihr Produkt zurückgibt
 */


/**
 * Schreibe eine Funktion `greet`, die:
 * - Einen Namen als Eingabe nimmt
 * - Eine Begrüßung im Format "Hallo, <name>!" zurückgibt.
 */

/**
 * Objekte
 */

/**
 *  1. Definiere eine Klasse `Person` mit den Eigenschaften `name` und `age` und einer Methode `introduce`, die einen String wie "Hi, ich bin <name> und bin <age> Jahre alt." zurückgibt.
 */

/**
 * 2. Schreibe eine Klasse `Employee` mit den Eigenschaften `name` und `salary`, und füge eine Methode `annualSalary` hinzu, die das Gehalt mit 12 multipliziert und in der Konsole ausgibt.
 */